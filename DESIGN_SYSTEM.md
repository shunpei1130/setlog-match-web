# set-mob モバイルデザインシステム

## 体験の定義

set-mobは、毎週土曜だけ開かれる「知らない青学生の一日を覗くイベント」。一般的なマッチングアプリではなく、土曜の過ごし方を起点にした小さな参加体験として見せる。Setlogは外部連携先としてのみ扱い、公式・公認サービスと誤認させない。

## Visual direction

- 編集的キャンパス。学生誌のような余白と、短い見出しを使う。
- モバイル390pxを基準にする。片手操作、縦スクロール、キーボード表示を優先する。
- 画面の主操作は1つ。説明文は短く、行動に必要なものだけ残す。
- すべてをカードに入れない。カードは状態・入力・候補など、まとまりが必要な場合だけ使う。
- 写真、外部フォント、グラデーション、強いドロップシャドウには依存しない。

## Tokens

```css
--paper: #f5f4ee;
--surface: #fffef9;
--surface-muted: #ebece4;
--ink: #163531;
--ink-soft: #64716b;
--line: #d7ddd5;
--accent: #d8674f;
--accent-dark: #a94838;
--success: #2f6b58;
--danger: #a94838;
```

- 余白: 4 / 8 / 12 / 16 / 24 / 32px
- 角丸: 12pxを基本に、主要な容器のみ16px。ボタンは12px。
- 文字: `-apple-system`, `BlinkMacSystemFont`, `Hiragino Sans`, `Yu Gothic UI`, `Meiryo`, sans-serif
- 見出し: 28〜42px、行間1.08、字間-0.05em
- 本文: 14〜16px、行間1.7
- 補助文: 11〜12px、行間1.55
- 罫線: 1px。状態の区切りに使い、装飾目的では増やさない。
- モーション: 160〜220msのopacity / transform。`prefers-reduced-motion`では停止する。

## Component rules

- 主ボタンは背景色のある12px角丸、最低48px高、幅100%を基本にする。
- 副ボタンは白地＋罫線。テキストボタンは安全導線など補助操作だけに使う。
- 入力欄は48px高、ラベルとエラーを必ず関連付ける。
- 状態表示は色だけに依存せず、テキストも表示する。
- 順位入力は数字ボタンを44px以上にし、選択状態を背景色・文字色・罫線で示す。
- 秘密投票は、選択内容が相手に見えないことを最初に伝える。
- ボトムシートは画面下から出し、閉じるボタンと背景クリックの両方を用意する。
- キーボード操作では`focus-visible`を常に表示する。

## Copy rules

- 英語の装飾ラベルを減らし、日本語の行動・状態を優先する。
- 「マッチ」より「土曜」「一日」「Day Pair」を使う。
- 断る・終わる・ブロックすることを失敗として表現しない。
- 内部順位、相手の回答、理由は表示しない。

## Visual reference

見た目のプリミティブは、MITライセンスの[Neo-Brutalism UI Library](https://github.com/marieooq/neo-brutalism-ui-library)を参考にする。

- 2pxの黒罫線
- 角のあるフラットな面
- 3〜7pxのハードシャドウ
- 朱色・黄色・コバルトの強い単色
- 少しだけ回転した紙面カード

set-mobではそのままのコンポーネント集合を使わず、「SATURDAY ISSUE」という紙面の世界観に変換する。アニメーションは必要な箇所だけCSSで行い、MVPに新しいUI依存を追加しない。
